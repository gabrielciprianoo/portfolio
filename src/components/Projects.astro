---
import { projects } from "../data/projects";
import ProjectCard from "./ProjectCard.astro";
---

<section class="bg-gray-900 text-white px-6 md:px-12 py-4">
  <div class="max-w-6xl mx-auto">
    <!-- Título -->
    <div class="mb-12">
      <p
        class="text-sm uppercase tracking-wide text-amber-400 font-semibold mb-2"
      >
        Proyectos destacados
      </p>
      <h2
        class="text-2xl md:text-5xl font-extrabold text-white mb-4 leading-tight"
      >
        Lo que he construido
      </h2>
      <p class="text-lg text-gray-300 leading-relaxed max-w-6xl">
        Aquí puedes ver algunos de los proyectos que he desarrollado aplicando
        tecnologías modernas y buenas prácticas de desarrollo web.
      </p>
    </div>

    <!-- Cards -->
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {projects.map((project) => <ProjectCard project={project} />)}
    </div>
  </div>
</section>
